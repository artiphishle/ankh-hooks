#!/usr/bin/env node

/*
import test from "node:test";
import assert from "node:assert";
import { useColorPalette } from "../hooks/color/useColorPalette";
import { act, renderHook } from "@testing-library/react";
*/

/*
test("returns valid Jewel Tone color in HSL", (t) => {
  const COLOR_COUNT = 4;
  const { result } = renderHook(() => useColorPalette({ hue: 240, saturation: [73, 83], lightness: [56, 76] }, COLOR_COUNT));
});
*/

/*
test("returns valid Pastel Tone color in HSL", () => {
  const COLOR_COUNT = 4;
  const { result } = renderHook(() => useColorPalette({ hue: 240, saturation: [14, 21], lightness: [89, 96] }, COLOR_COUNT));
});
*/